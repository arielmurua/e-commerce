export const validations = {
  loginEmailUser: () => {
    return (e) => {
      console.log(e.target.name)
      // let errors = {}
      // if (e.target.value.length < 3) {
      //   errors[e.target.name] = "username debe ser mayor a tres letras"
      // } else {
      //   errors = {}
      // }
      // return errors
    }
  }
}